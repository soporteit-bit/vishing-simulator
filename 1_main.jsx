import React from 'react'
import ReactDOM from 'react-dom/client'
import VishingSimulator from './vishing-simulator.jsx'

// Render the app
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VishingSimulator />
  </React.StrictMode>,
)
